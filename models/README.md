# Model artifacts

Trained model files are ignored by Git by default because model artifacts can leak information about training data. Reconsider this policy only after approval from the laboratory/data owner.
