# Private data directory

Place the full laboratory datasets here **locally only**. This directory is ignored by Git.

Expected filenames are configured in `config/default.yaml`. You may also keep the data outside the repository and set `PREGNANCY_DATA_DIR` to that directory.

Do not commit patient-level medical records to this repository.
