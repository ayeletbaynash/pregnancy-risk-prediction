# Synthetic sample data

These CSVs contain entirely fictional records generated only to demonstrate the expected input structure. They are **not** de-identified extracts from the research datasets and contain no patient records.

Run the public demo pipeline with:

```bash
python run_pipeline.py --sample
```

The sample is intentionally tiny and is intended to validate code flow, not to reproduce research performance.
