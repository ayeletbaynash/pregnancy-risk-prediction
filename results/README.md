# Results

This directory is intended for aggregate, non-identifying outputs such as evaluation metrics and figures.

`results/predictions/` is Git-ignored because it contains row-level predictions when the pipeline is run on the private medical datasets. Review all aggregate outputs with the laboratory/data owner before publishing them.

The repository template does not ship synthetic-run figures, because those could be mistaken for research results.
